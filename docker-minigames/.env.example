# MongoDB Configuration
# For local MongoDB
MONGODB_URL=mongodb://localhost:27017/docker_quiz_game

# For MongoDB Atlas (replace with your actual connection string)
# MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/docker_quiz_game?retryWrites=true&w=majority

# Database name (optional - will be extracted from URL if not provided)
DB_NAME=docker_quiz_game

# Application Environment
ENVIRONMENT=production

# MongoDB Performance Settings (optional)
MONGODB_TIMEOUT=10000
MONGODB_POOL_SIZE=10
